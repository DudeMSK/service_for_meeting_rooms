# Контекст проекта «Бронирование переговорных комнат»

Полный рабочий контекст проекта ведется в @INFO_CONTEXT.md — этот файл общий для Claude Code, ChatGPT/Codex и любого другого ассистента, поэтому весь содержательный текст находится там, а не здесь.

Этот файл — только короткая заглушка-импорт, нужная для того, чтобы Claude Code автоматически подхватывал контекст проекта из `INFO_CONTEXT.md`. Все обновления по проекту (новые требования, решения, изменения кода/конфигурации/версии/сборки) вноси в `INFO_CONTEXT.md`, а не сюда.
